[package]
name = "runtimescaner"
version = "0.1.1"
edition = "2021"
description = "Linux runtime shared library audit CLI"
license = "MIT OR Apache-2.0"
repository = ""
readme = "README.md"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
strip = true
